name: Star chart

on:
  schedule:
    - cron: "0 6 * * 0" # 每周日 UTC 06:00
  workflow_dispatch:

permissions:
  contents: write

jobs:
  star-chart:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: jal-co/shieldcn@v1
        with:
          theme: violet
